/**
 * Reference to core stack
 */
export const coreStackRef = 'bjerk-simen/tabetalt-core';

/**
 * Primary domain
 * eg. {env}.tabetalt.no
 */
export const primaryDomain = '{env}.{version}.api.tabetalt.no';

/**
 * Google Organization Name
 */
export const organizationName = 'tabetalt.no';

/**
 * Default administrator email
 */
export const adminEmail = 'so@bjerk.io';

/**
 * Google Cloud Platform Folder ID
 */
export const folderId = 'folders/92480390775';

/**
 * Default region
 */
export const region = 'europe-west1';

/**
 * Default zone
 */
export const zone = `${region}-b`;

/**
 * Core Managed Zone
 */
export const coreZone = 'tabetalt-no';

/**
 * Default version
 */
export const version = 'v1' || process.env.VERSION;

/**
 * Github Organization
 */
export const githubOrg = 'tabetalt';
/**
 * Github Admin
 */
export const githubAdmin = 'cobraz';
